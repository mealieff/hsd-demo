# Data directory

Download the **Gab Hate Corpus** from https://osf.io/edua3/ and place these files here:

- `ghc_train.tsv`
- `ghc_test.tsv`

Each TSV has columns: `text`, `hd` (hate speech), `cv` (calls for violence), `vo` (vulgar language). Values are 0/1.
